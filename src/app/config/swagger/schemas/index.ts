/**
 * @openapi
 * components:
 *   schemas:
 *     UserPermissionAttributes:
 *       type: object
 *       properties:
 *         userId:
 *           type: number
 *         permissionId:
 *           type: number
 *     ScreenPermissionAttributes:
 *       type: object
 *       properties:
 *         screenId:
 *           type: number
 *         permissionId:
 *           type: number
 *     ScreenAttributes:
 *       type: object
 *       properties:
 *         id:
 *           type: number
 *         name:
 *           type: string
 *         description:
 *           type: string
 *         url:
 *           type: string
 *         createdAt:
 *           type: string
 *           format: date-time
 *         updatedAt:
 *           type: string
 *           format: date-time
 *     PermissionAttributes:
 *       type: object
 *       properties:
 *         id:
 *           type: number
 *         description:
 *           type: string
 *         title:
 *           type: string
 *         createdAt:
 *           type: string
 *           format: date-time
 *         updatedAt:
 *           type: string
 *           format: date-time
 *     UserAttributes:
 *       type: object
 *       properties:
 *         id:
 *           type: number
 *         username:
 *           type: string
 *         email:
 *           type: string
 *         password:
 *           type: string
 *         name:
 *           type: string
 *         createdAt:
 *           type: string
 *           format: date-time
 *         updatedAt:
 *           type: string
 *           format: date-time
 */
